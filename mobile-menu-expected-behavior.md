# モバイルハンバーガーメニュー - 想定する正常動作

## 📱 理想的なユーザー体験フロー

### 1. 初期状態（モバイルでアクセス時）
- [x] 画面幅 < 768px でアクセス
- [x] サイドバーは非表示（デスクトップ用は hidden）
- [x] 右下にハンバーガーメニューボタンが浮遊表示
- [x] メインコンテンツがフル幅で表示
- [x] 「ようこそ！左側のフォルダから...」メッセージ表示

### 2. ハンバーガーメニューボタンタップ時
- [x] ボタンに視覚的フィードバック（scale-95効果）
- [x] サイドバーが画面左から 300ms でスライドイン
- [x] 半透明の黒いオーバーレイが背景に表示
- [x] 本文スクロールが無効化（overflow: hidden）
- [x] サイドバー内にフォルダ一覧とファイルツリー表示

### 3. サイドバー表示状態
- [ ] サイドバー幅: 320px（画面幅の85%以下）
- [ ] フォルダアイコン付きでファイル階層表示
- [ ] 検索ボックスが機能する
- [ ] フォルダクリックで展開・折りたたみ
- [ ] ファイルクリックでメインエリアに内容表示
- [ ] サイドバー内でスムーズスクロール

### 4. サイドバーを閉じる動作
- [x] オーバーレイタップ → サイドバーが左にスライドアウト
- [ ] ハンバーガーメニュー再タップ → サイドバーが閉じる
- [ ] ESCキー押下 → サイドバーが閉じる（キーボード対応）
- [x] 本文スクロールが再有効化
- [x] オーバーレイが消失

### 5. ファイル選択時の挙動
- [ ] サイドバー内のファイルをタップ
- [ ] サイドバーが自動的に閉じる
- [ ] メインエリアにドキュメント内容表示
- [ ] パンくずナビゲーション表示
- [ ] 「戻る」ボタンでファイル一覧に戻る

### 6. レスポンシブ対応
- [ ] 画面回転時に適切にレイアウト調整
- [ ] デスクトップ表示時（768px以上）にハンバーガーメニュー非表示
- [ ] デスクトップ時は通常のサイドバー表示に自動切替
- [ ] 縦長・横長どちらでも操作性維持

## 🎯 成功判定の具体的指標

### 視覚的確認項目
- [x] ボタンタップでサイドバーが**見える**
- [x] アニメーションが**スムーズ**（カクつきなし）
- [x] オーバーレイが**適切な透明度**で表示
- [x] フォルダアイコンと階層が**正しく表示**

### 操作性確認項目
- [x] タップ反応が**即座**（遅延なし）
- [x] スクロールが**自然**（慣性あり）
- [ ] ファイル選択が**直感的**
- [x] 閉じる操作が**複数の方法**で可能

### 技術的確認項目
- [x] JavaScriptエラーが**発生しない**
- [ ] メモリリークが**ない**
- [ ] CPU使用率が**適正**
- [ ] 異なるブラウザで**統一動作**

## 🔧 実装とテストの繰り返し方針

### 段階的実装
1. **基本動作**: ボタンクリック → サイドバー表示
2. **アニメーション**: スムーズなスライドイン/アウト
3. **オーバーレイ**: 背景クリックで閉じる機能
4. **ファイル操作**: サイドバー内での選択機能
5. **レスポンシブ**: 画面サイズ変更対応

### テスト手順
1. **ローカル実装** → **ブラウザ開発者ツールでテスト**
2. **GitHub へコミット** → **自動テスト実行**
3. **実機での確認** → **問題発見**
4. **修正実装** → **再テスト**
5. **全項目クリアまで繰り返し**

## 📝 現在の状況
- **実装済み**: HTMLマークアップ、基本CSS、JavaScript骨格
- **未動作**: ボタンクリックでサイドバーが表示されない
- **次のステップ**: JavaScript動作の根本的な確認と修正

---
*この仕様通りに動作するまで実装とテストを繰り返す*